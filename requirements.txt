fastapi
uvicorn[standard]
python-multipart
transformers
torch 
torchvision
Pillow
pandas
pytesseract
timm
accelerate>=0.20.3 # Accelerate can work with CPU only
huggingface_hub
matplotlib # Keep if essential, remove if only for local plotting & not used in deployed app
