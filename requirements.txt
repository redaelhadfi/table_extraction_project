fastapi
uvicorn[standard]
python-multipart
transformers
Pillow
pandas
pytesseract
timm
accelerate>=0.20.3 # Or pin to the version you used in development if different
huggingface_hub
# matplotlib # Optional: remove if not used by your deployed FastAPI app to save space

# --- PyTorch CPU-only Configuration ---
# Instruct pip to find CPU-specific wheels from the official PyTorch index.
# Pip will select the appropriate version for your Python runtime on Heroku.
torch --index-url https://download.pytorch.org/whl/cpu
torchvision --index-url https://download.pytorch.org/whl/cpu
# torchaudio --index-url https://download.pytorch.org/whl/cpu # Add if you explicitly use torchaudio